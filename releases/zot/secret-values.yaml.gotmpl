---
externalSecrets:
  zot-htpasswd:
    refreshInterval: "1h"
    clusterSecretStoreName: css-{{ .Values.cluster }}
    target:
      creationPolicy: Owner
      deletionPolicy: Delete
    data:
      - secretKey: htpasswd
        remoteRef:
          key: zot/htpasswd
          property: htpasswd
